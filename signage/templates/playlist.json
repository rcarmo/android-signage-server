{
    "uuid": "{{ view.uuid }}",
    "name": "{{ view.name }}",
    "alerts": false,
    "assets": [
        {% for asset in view.assets %}
        {
            "name": "{{ asset.name }}",
            "url": "{{ asset.url }}",
            "duration": "{{ asset.duration }}"
        }{% if not forloop.last %},{% endif %}
        {% endfor %}
    ]
}